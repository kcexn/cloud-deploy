---
- name: Update OS packages
  ansible.builtin.package:
    update_cache: yes
    upgrade: yes

- name: Install common packages
  ansible.builtin.package:
    name: "{{ common_packages }}"
    state: present
